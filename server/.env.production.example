# Production Environment Variables for Railway

# Server
PORT=5000
NODE_ENV=production

# MongoDB Atlas
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/expense-sharing?retryWrites=true&w=majority

# JWT Secret (use a strong random string!)
JWT_SECRET=your-super-secret-jwt-key-minimum-32-characters

# Redis (Railway provides this automatically if you add Redis addon)
REDIS_URL=redis://default:password@host:port

# CORS - Your frontend domain
CORS_ORIGIN=https://www.runelabs.dev,https://runelabs.dev

# Balance Service
USE_BALANCE_V2=true
